# I2C2 测试
## 步骤

1. 冷启动后执行 `ls /dev`，确认 `/dev/i2c2`。
2. 检查启动日志中 GT911 地址 `0x5d` 和 `/dev/input0` 注册结果。
3. 完成 10 次冷启动，确认无 I2C 超时和控制器卡死。
4. 结合触摸原始数据验证 I2C2 的持续读链路。

## 预期

- I2C2 每次均注册成功，GT911 可读取且不会拖死系统任务。
- 若当前 NSH 配置没有通用 I2C 扫描命令，必须注明“未提供直接扫描能力”，
  不以猜测地址代替结果。
