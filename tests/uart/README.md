# UART0 测试
## 步骤

1. 使用 115200 8N1、无软硬件流控连接 UART0。
2. 执行 `echo RX_OK`、`help`、`ls /dev`、`ifconfig`，确认输入回显和输出完整。
3. 连续执行 100 轮短命令；同时观察网络、触摸和米家日志输出。
4. 在长日志输出期间继续输入命令，检查丢字符、乱码、锁死和命令粘连。

## 预期

- 输入命令与回显一致，NSH 始终可交互。
- 并发日志不会永久破坏命令输入，UART 不发生中断风暴或系统异常。
- 所有乱码都要记录原始字节、发生时间和当时的系统负载。
