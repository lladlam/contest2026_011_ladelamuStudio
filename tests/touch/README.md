# GT911 触摸测试
## 步骤

1. 执行 `hexdump /dev/input0 count=32`，分别记录按下、移动和释放样本。
2. 测试四角、四边和中心区域，确认坐标方向和有效范围。
3. 对导航、房间、设备卡、返回键、开关和滑块执行至少 500 次操作。
4. 在米家同步、DHCP 重连和页面刷新期间继续触摸。

## 预期

- 坐标与画面方向一致，无永久按下、幽灵点击和空样本阻塞。
- 开关一次点击只触发一次命令；滑块拖动跟手，释放后才发送最终值。
- 500 次操作后输入和 UI 仍可用，异常次数为 0。
